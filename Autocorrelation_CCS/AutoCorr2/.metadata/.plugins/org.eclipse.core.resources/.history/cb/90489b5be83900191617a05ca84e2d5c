/*
 * main.c
 */

#include <stdio.h>
#include <math.h>
#include <string.h>


int AutoCorrelation(int *tabSing, int l);

int main(void) {

    int sing[4] = {1,-1,1,-1};
    int l = sizeof(sing) / sizeof(sing[0]);
    l = 4 ;

    int longueur = l + (l - 1)*2;
    int hauteur = (l * 2) - 1;


    AutoCorrelation(sing, l);

    return 0;
}
